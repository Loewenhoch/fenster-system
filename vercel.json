{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && prisma db push --skip-generate && npx tsx scripts/seed-postgres.ts && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "AUTH_URL": "https://fenster-system-sta.vercel.app",
    "NEXTAUTH_URL": "https://fenster-system-sta.vercel.app"
  }
}
